import React from "react";
import "./Friendslist.css";

export const Friendslist = ({ children }) => {
  return (
    <div className="col s3 center card-panel">Friendslist</div>
  );
};

export default Friendslist;