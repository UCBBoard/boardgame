import React from "react";
import "./Newsfeed.css";

export const Newsfeed = ({ children }) => {
  return (
    <div className="col s3 center card-panel">Newsfeed</div>
  );
};

export default Newsfeed;